{% extends "::base.html.twig" %}

{% block title %}AppBundle:Team:add{% endblock %}

{% block body %}
    <div class="row no-margin">
        <div class="col-md-4 col-md-offset-1">
            <form method="post" action="{{ path('create_myTeam') }}" {{ form_enctype(form) }} novalidate>
                {{ form_start(form) }}
                {{ form_errors(form) }}
                {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">Utwórz</button>
            {{ form_end(form) }}

            {{ form_rest(form) }}
            </form>
        </div>
    </div>
{% endblock %}
