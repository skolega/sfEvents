{% extends "::base.html.twig" %}

{% block title %}AppBundle:Team:add{% endblock %}

{% block body %}
    <div class="row no-margin">
        <div class="col-md-4 col-md-offset-1">
            <form method="post" action="{{ path('create_myTeam') }}" novalidate>
                {{ form_start(form) }}
                {{ form_errors(form) }}
                
            <button type="submit" class="btn btn-primary">Utwórz</button>
            {{ form_end(form) }}

            {{ form_rest(form) }}
            </form>
        </div>
    </div>
{% endblock %}
